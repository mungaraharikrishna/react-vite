const routePaths = {
    admin: '/admin',
    user: '/user',
    auth: '/user/auth',
    login: '/user/auth/login',
    dashboard: '/admin/dashboard',
}

const navigationPaths = {
    routePaths,
};

export default navigationPaths;